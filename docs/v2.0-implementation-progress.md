# RT-DWH 2.0 实施进度

更新：2026-09-05。对应 [2.0 开发计划](v2.0-product-iteration-plan.md)。

本文件区分代码实现、自动化验证和真实环境验收。2.0 全部工作包尚未完成；当前改动已部署到本地 Docker 服务。

## 当前交付

### V2-01：可靠性与权限修复

代码已实现，自动化回归通过；真实 CDC／Doris 查询、Compact 成功与失败、Gateway 断连恢复及双角色 HTTP 范围检查已通过。超时路径使用单条测试记录的时间注入验证。

- 旧依赖兼容路径检查同业务日期最新上游实例及其实际产出。上游执行成功但产出 blocked、缺少应有产出、最新执行尚未成功时不放行；移除草稿产出不会消除已记录的 blocked。V24 新实例改用下述明确绑定。
- 维护改为持久化的 SESSION → CATALOG → USE → CALL → JOB（如有）流程。使用 Gateway 的 sessionHandle／operationHandle，只有确定执行终态后才关闭会话；返回 Job ID 时继续核对 Flink Job。
- Gateway 中断、提交回包丢失记录 unknown；观测超时记录 timed_out 并继续协调。没有操作句柄的模糊提交不会自动重试，避免重复清理。历史 running 记录迁移为 unknown，不伪造成功。
- 每个维护会话初始化平台 Paimon JDBC Catalog。Compact 使用命名 compact_strategy 参数，修复把策略误放到分区参数的问题，依据 [Paimon 2.0 Procedures](https://paimon.apache.org/docs/2.0/flink/procedures/)。
- 血缘按照用户可访问任务和表构建；持久化边、关键词扩展、SQL 注释解析不会重新引入无权表。
- 数据服务列表、修改、发布、删除、授权及日志增加数据范围检查。调用应用仅管理员或创建者可管理，包括密钥轮换、停用、授权和撤销。
- 表详情、字段、快照、注释、治理编辑、维护及日志入口补齐对象范围检查。批量维护先校验整个范围，再开始执行；全局元数据同步限管理员。
- 维护页面显示待人工执行、待协调、超时协调和真实终态，周期刷新日志；受理提示不宣称执行完成。

### V2-02：周期任务发布契约（首个子包）

周期发布契约已实现并验证，后续子包见下文。

- 保留原任务 snapshotJson，新增 contractJson 与 SHA-256 contractHash，冻结依赖、产出声明、SLA 配置和门禁规则定义／版本。
- 创建调度／补数实例、释放依赖和登记产出读取绑定版本的契约。新契约损坏时拒绝执行，不回退读取草稿。
- 产出检测执行冻结规则；草稿关闭门禁不会改变在途实例。更新最新产出时间时只修改运行字段，不把旧快照覆盖回草稿配置。
- 内置 Runner 读取任务快照时重验提交者当前 SQL／产出权限，并记录 accessCheckedAt。历史版本列表、下载实例定义和回滚也校验旧版本涉及的数据范围。
- 依赖、产出编辑标记草稿有变更；发布页面说明重新发布的生效边界，版本列表展示契约来源。
- 旧版本明确标为 legacy-inferred，保留兼容读取，不回填不存在的历史契约证据。新发布版本标为 frozen-v1。

### V2-02：持续任务发布与部署修订

- 持续任务新增发布、版本列表、部署历史和详情页入口。首次启动兼容自动发布；后续启动读取已发布版本，修改草稿不改变启动定义。
- CDC 发布冻结 SQL／表结构，密码使用受控占位符，执行时绑定当前凭证。恢复沿用原部署版本及保存点。
- 提交前独立持久化部署意图，记录版本、契约指纹、并行度、Job ID 和恢复路径；结果不明时阻止盲目重复启动，定期与 Flink 状态协调。
- 启动及历史读取重新校验 SQL／映射目标和冻结产出权限。

### V2-02：类型化参数、调度修订、运行配置与权限审计

- 新发布任务固定 `parameterSchemaJson`，支持 string／integer／number／boolean／date／datetime、必填与默认值。创建补数／调度实例时按发布契约校验并落下实际参数，默认值不再随草稿漂移。
- `${name}` 只渲染完整值；兼容 `'${bizdate}'` 与旧版本的标量值写法，拒绝标识符拼接、片段替换、错误类型和未知参数，参数值不递归展开。旧任务没有类型定义时，重新发布前需要补齐参数契约。已发布旧实例保留兼容读取，但同样使用安全字面量渲染。
- 新增 `task_schedule_revision`，保存 Cron、时区、业务日期偏移、参数、启用状态、修改人及动作。每次配置／删除保留记录，实例固定 `scheduleRevisionId` 与 `scheduledAt`；执行身份来自对应修订的修改人。历史调度首次触发时只采用当时可观察配置，不伪造过去的修订。
- 新增版本内运行配置 JSON 与指纹，涵盖 Gateway／Flink／Doris 端点、Paimon Catalog／Warehouse 及受控凭证引用。执行前检查当前配置一致性，地址或 Warehouse 漂移需重新发布；凭证轮换通过引用解析，明文密码不进入新发布快照。
- 运行配置指纹覆盖代码列举的配置项与 JDBC 安全选项；不代表已认证镜像 digest、所有任意连接参数或引擎能力。跨环境自动映射仍未提供。
- 新增独立事务权限检查记录，持久化执行身份、发布版本、实例、动作、结果与时间；在真正提交前对渲染后的 SQL 和冻结产出重验范围。界面增加参数契约、调度历史和权限检查入口。
- Savepoint 联调修复：使用 Flink 的 `request-id`、`operation.location`、`operation.failure-cause`，区分请求完成与执行失败；停止作业不会抢先清除保存点句柄，手动保存点仍保持运行。不再在不确定的停止响应后自动调用第二个端点，接口依据 [Flink 2.2 REST API](https://nightlies.apache.org/flink/flink-docs-release-2.2/docs/ops/rest_api/)。

### V2-03：交付状态、依赖绑定、补数策略与执行尝试

核心控制面已实现并完成本地 HTTP／Flink 验证；提交后安全重放及物理数据版本隔离仍未认证。

- 计算 success 后进入独立的 deliveryStatus=checking，由交付协调器执行冻结质量规则并登记 available／blocked。暂时性持久化故障保留计算成功，等待交付恢复；规则失败和检测异常保留明确原因。
- 新实例固定按日业务窗口 `[businessDate, 次日)`。产出记录增加定义版本、attempt、窗口、质量批次及检测时间，不用历史空字段推断可信证据。
- 数据依赖显式指定上游产出，按版本、窗口及 productionId 绑定；纯控制依赖显式选择 execution_success。已绑定的产出不随最新记录漂移，领取和提交前再次校验条件，提交时重验上游版本权限。
- 补数默认 batch_only，递归创建同批上游祖先任务；显式 reuse_available 才使用同版本、同窗口的已有可用产出。支持上游参数映射并限制批次规模，创建失败整体回滚。
- 每次领取产生独立 attempt，持久化执行器、提交意图、Job ID、状态和错误。提交意图必须先于外部调用；旧 attempt、错误执行器、未登记意图的 Job 绑定均被拒绝。历史尝试和 Job ID 保留。
- 完成回调使用实例行锁，并刷新请求内可能已加载的实体；重复完成保持相同终态和完成时间。交付登记在独立事务内通过行锁和唯一键去重，回调不会重复执行质量检测。
- 仅确定没有提交意图／Job ID 的失败尝试可重试；重新领取创建新 attempt。提交结果不明、Job 丢失及提交后租约过期保留 unknown，不自动重放，也不能把不明写入直接标为已取消。租约回收加锁后再次核对当前心跳。
- blocked 实例可按原规则重检，复用 productionId 并追加检测历史。重检不把检测时间作为新的产出时间，不覆盖更晚的最近产出实例。
- 页面展示计算／交付状态、执行尝试、依赖绑定、补数策略和检测历史；首次产出被阻断时仍可查看记录。日期数组按业务日期格式显示。

接口接入与兼容行为见 [周期执行与数据交付契约](workflow-execution-contract.md)。

### V2-04：资产身份、Schema 修订和生产消费入口

Paimon 资产基础已实现，真实分层样例、结构演进及新增接口的双角色本地 HTTP 回归均已通过。

- Flyway V25 为旧表补齐不可变 UUID，保留数字 ID；表登记保存 Catalog、主键／追加类型、发现状态、Schema 状态与观测时间。新产出契约和实际 production 关联 assetId。
- 完整 Catalog 不再发现曾观测表时标记 missing，保留字段与修订；只声明、未实际发现的表维持 unverified。查询 Catalog 失败或 Schema 不可读时不抹除原字段。
- 字段按 Paimon engine field ID 合并，重命名保留平台字段 ID、业务注释及源字段关联；同名但 ID 不同视为替换。结构指纹去重，记录 baseline／compatible／risk／breaking 修订及前后差异。
- 资产详情集中展示可见发布版本的生产／消费任务、当前报表和服务、关联资产、真实产出及质量检测批次。关系依据区分冻结契约、映射和 SQL AST；草稿修改不覆盖发布关系。
- 稳定资产 URL、列表筛选与页签上下文、资产到即席查询及返回入口已接入；查询通过显式载入生成预览 SQL，不自动执行。
- 真实加工样例发现并修复周期 SQL 新会话缺少平台 Catalog 初始化的问题。内置执行器在发布契约／权限校验后初始化受控 JDBC Catalog、ODS 默认库及 batch 模式；密码仅在提交时解析，原发布 SQL 保持不变。
- 当前 UUID 代表逻辑表登记，同名重建的物理身份连续性、表重命名后的显式重绑定、复杂类型兼容矩阵及全量影响分析仍未实现。现有独立血缘图未切换到新 AST 路径；普通 View 的首个子包见 V2-05。

规则、接口边界与样例复现见 [资产与 Schema 契约](asset-contract.md)。

### V2-05：普通 Doris View（首个子包）

已实现受控 DDL、草稿/发布版本隔离、递归依赖权限和统一资产入口。该子包保守固定已发布 View 的输出列与直接依赖集合，尚未达到 V2-05 全量 GA 范围。

- Flyway V26 将资产地址唯一键扩展为 Catalog/Database/Name；Doris View 与 Paimon 表共享 UUID 资产目录，旧 Paimon 元数据和维护接口按类型隔离。
- View 位于 `internal.rtdwh_views`。定义只接受可完整解析的 SELECT 子集和三段依赖；首次发布不覆盖已有物理对象，后续发布核对冻结 Doris 定义、字段契约与当前依赖权限。
- 发布意图先提交管理库，再执行 CREATE/ALTER。DDL 结果不明保留 applying/unknown 并阻断后续使用，不自动重复提交。发布历史保存 SQL、依赖、输出列和发布者；草稿修改不改变线上版本。
- 即席查询、报表和 API 均在 Doris 专用校验入口展开托管 View，检查每层及基础表权限、缺失/循环/定义漂移/字段变化，限制展开规模。管理员同样须通过定义完整性检查；工作台保持只读。
- 已发布 View 的列契约或直接依赖集合变化被预览及发布接口同时阻止，需要使用新名称迁移。资产上下游关系加入已发布 View 的直接依赖；完整传递影响分析、变更迁移和未知 DDL 的人工接管入口仍待后续子包。
- 公共检索按资产实际 Catalog 过滤，报表与服务检索展开 View 依赖；旧血缘图继续限制为 Paimon 表，避免内部 View 被按 Paimon 范围展示。
- 真实验证修复 MySQL JSON 格式规范化导致的相同字段契约误判；角色范围更新先 flush 删除，再插入保留范围，修复部分撤权的唯一键冲突。

接口、支持的 SQL 子集及限制见 [普通 View 发布契约](managed-view-contract.md)。

### V2-08 首期：菜单与窗口质量

按本轮优先级提前实施质量基础，能力分期与验收契约见 [数据质量能力规划](data-quality-capability-plan.md)。完整 SLA / 通知处置 / 豁免仍待开发。

- 菜单调整为工作台、资产检索与治理、数据开发、数据资产、质量工作台、数据消费、平台管理；同步页面标题与中英文词条，保留原 URL 与权限。侧栏“存储维护”避免长名称截断，页面继续明确 Paimon。
- 四类规则增加全表 / 业务窗口、业务时间字段与空数据 fail/allow；手动指定日期，全局定时按配置时区前一日（默认 Asia/Shanghai），产出联动按实例实际窗口。
- Flyway V27 保存冻结目标、版本、窗口、空数据策略、检测行数与异常计数；一次 SQL 聚合产生一致证据。范围表达式 false / NULL 都计为无效；空数据默认失败。
- 预览 SQL 不执行检测或保存规则。页面独立提供概览、质量规则、异常告警、执行记录，保留日期/页签/资产筛选 URL，资产详情可进入规则页。无数据不显示正常，当前版本覆盖率与通过率替代综合健康分。
- 告警按规则+窗口去重与自动恢复，某日通过不关闭另一日异常；人工确认不放行门禁。历史按冻结目标及 View 底层权限鉴权，规则改目标/删除不能转移历史访问权。
- 修复真实 HTTP 执行时 OpenEntityManagerInView 自动刷新运行状态导致 CAS 完成失败的问题：执行器使用已脱离持久化上下文的运行快照，终态和告警继续原子提交。
- `scripts/smoke-quality-windows.py` 在 Docker 真实通过：SQL 预览、精确日期边界、NULL 无效、窗口外坏数据排除、空数据 fail/allow、按日期恢复；真实 Flink 有界写入与冻结窗口门禁、blocked 原产出重检；两个精确范围角色、改目标/删规则后的历史隔离、托管 View 底层权限撤销后的执行与历史阻断。
- 浏览器验证窗口规则填写/预览/保存/检测和执行 SQL/行数证据；检查 1366×768、1440×900、1920×1080 的质量页面布局。补齐浅色异常提示和弹窗 SQL 换行，避免提示背景与文本对比不足。


## 验证证据

- 后端 service 和 controller 测试：216 个，失败 0、错误 0、跳过 0（当前源文件对应的测试报告；不计 target 中旧报告）。覆盖 attempt 隔离、租约竞态、模糊提交、重复回调、交付恢复、绑定策略及重检新鲜度；新增窗口边界、空数据策略、告警按窗口隔离、历史范围及定时时区跨日验证；原有资产/发布/运维回归保持通过。
- 前端生产构建通过；TypeScript `tsc --noEmit` 通过。浏览器已验证参数契约、补数默认策略、计算／交付分态、执行尝试、窗口及具体依赖绑定、首次 blocked 产出记录和重检历史；三档分辨率完整回归仍在 V2-10。
- V20、V21 已在独立、无网络、无宿主数据卷的 MySQL 8.0 临时容器中执行，验证 ENUM 扩展、旧运行状态迁移、旧任务快照保留及新增字段；测试容器已删除。这是新增迁移的局部验证，尚非完整应用升级演练。
- 本地现有数据库从 Flyway V18 升级到 V27，初始升级前备份位于忽略目录 `tmp/backups/pre-v2-local.sql`，V24 前另存 `tmp/backups/pre-v24-local.sql`。V25 前另存 `tmp/backups/pre-v25-local.sql`。V26 前另存 `tmp/backups/pre-v26-local.sql`。V27 前另存 `tmp/backups/pre-v27-local.sql`（0600）。V27 迁移成功。
- 真实增强冒烟通过：发布后草稿并行度从 1 改为 2，部署仍绑定原版本及并行度 1；MySQL 增量事件可经 Doris 查询；测试表 Compact 记录确认 success。测试任务与数据源按脚本清理。
- 本地环境访问与复现方式见 [Docker 开发环境](local-docker-development.md)。
- `scripts/smoke-release-contract.py` 已通过真实 HTTP＋Flink：错误参数拒绝、修改草稿默认值仍执行已发布值、实例成功及权限检查落库、实际调度触发绑定修订、删除调度保留历史。
- V24 部署后再次运行真实发布契约冒烟通过：内置执行器完成 Flink 作业，独立交付最终为 available，调度实例仍绑定正确修订。9 个项目容器运行中，后端健康 UP，内置工作流执行器已恢复启用。
- `scripts/smoke-workflow-delivery.py` 通过本地 HTTP：6 个并发领取请求仅一个成功，8 个并发完成请求保持同一完成时间且每个产出只登记一次／检测一次；blocked 数据依赖等待，控制依赖及所选 available 产出依赖放行；本批补数不借用旧批次；复用不串发布版本；草稿关闭门禁后原实例重检仍 blocked 且保留两次检测；提交前重试创建新 attempt，旧回调和提交后重试被拒绝。该脚本模拟受授权执行器回调，不宣称它执行了真实写入。专用任务和模拟资产已清理。
- `scripts/smoke-control-plane.py --gateway-fault` 已通过：两个互斥范围角色的资产详情／字段／快照、任务、血缘、数据服务列表及操作负向检查；原令牌撤权立即生效；真实 CALL 失败；Gateway 暂停后 unknown、单条测试记录注入超时时间后 timed_out、恢复后原操作 success。测试容器已恢复，临时角色／用户／任务／服务／元数据已清理。
- 增强 CDC 恢复冒烟已通过：停止任务生成真实 Savepoint，随后发布另一版本，恢复修订仍绑定原版本及恢复路径；恢复后写入更新，Doris 查询结果与源表一致。真实验收定位并修复了保存点字段解析和 Job 终态抢先清除句柄的问题。
- `scripts/smoke-asset-contract.py --keep` 已完整通过真实 HTTP／Flink／Paimon／Doris：有限 VALUES 输入到 ODS → DWD → ADS 三层作业、batch_only 绑定交付、冻结质量门禁、报表结果 60、生产／消费／质量关系、草稿隔离、实际增列／字段改名／删列分级、字段注释身份保留、无变更去重及暂时缺失后重新发现。
- 生产浏览器已验证 Schema 历史与前后对照、发布生产消费关系及质量批次弹窗、资产到即席查询的 SQL 载入／真实两行结果及返回原 Schema 页签、缺失警示和返回列表保留搜索条件。资产总览统计排除 missing／unverified 登记，避免同名暂时改名的历史记录重复计数。修复了 Monaco 模块合并导致的运行时引用错误；关闭当前构建的 concatenateModules，保留代码拆分和压缩。
- 本轮专用资产样例任务、报表、服务、规则及物理测试表均已清理；查询与审计历史按平台正常策略保留。9 个项目服务运行中，健康检查 UP，内置执行器启用，部署 jar 与通过测试的产物 SHA-256 一致。
- V25 最终后端再次通过发布参数／黑洞目标／实际调度 HTTP＋Flink 回归，确认平台 Catalog 初始化保留既有契约行为。
- 2026-09-05，用户授权后完整运行扩展的 `scripts/smoke-control-plane.py`：两个互斥范围角色可读取自己的资产 UUID 详情／context／schema-revisions，读取对方资产被拒绝；旧数字 ID、字段、快照、任务、血缘与数据服务的越权读取和操作检查通过；修改角色范围后，原登录令牌立即失去对应资产及任务访问权。真实 Paimon CALL 失败仍记录为 failed。脚本正常退出，并通过数据库计数确认临时用户、角色、任务、服务和资产均为 0；后端 UP，9 个项目容器运行正常。本次未启用 `--gateway-fault`，断连实验仍对应上文 V24 验收。关系模块权限和历史发布版本可读性另有后端测试覆盖。
- V26 最终版本的 `scripts/smoke-managed-views.py` 完整通过：两个专用 Paimon 表与精确范围角色、统一检索自身可见/对方隐藏、普通及嵌套 View、草稿隔离、不可变历史、兼容重发布、输出列变更拒绝、报表和应用 API 实际返回 31；保留 View 权限并撤销基础表权限后，原令牌的查询/报表/API 发布/应用调用均被阻断；依赖暂时缺失、未托管 View 和工作台 DDL 被拒绝。修复后的角色范围更新可完成部分撤权并恢复。
- 浏览器完成新建 View 草稿、校验预览及受控发布（追加 V3）、查看仍保持原 SQL 的 V1、嵌套 View 下游和基础表上游入口、查询 View 返回 31，以及返回后保持“下游使用”页签。修复前端新接口 API 前缀遗漏。普通 View 不展示 Paimon 文件维护或产出交付卡片；完整多尺寸回归仍在 V2-10。
- V26 再次通过原双角色控制面及真实 Flink 发布/调度契约回归。最终清理后，专用 View/Paimon 样例资产、用户、角色、报表、服务和应用计数均为 0；Doris 仅保留原有两张 ODS 表，托管 View 数据库为空。9 个项目服务运行、后端 UP、内置 Runner 启用；部署 jar SHA-256 与当前测试产物一致，V26 前备份为 0600。
- 本机 Mockito 动态 self-attach 不可用，测试使用 JVM 启动时加载现有 Byte Buddy agent；没有关闭或削弱断言。

本机复现测试命令（从仓库根目录；agent 路径需匹配本机 Maven 仓库）：

```sh
mvn -o -f backend/pom.xml \
  '-DargLine=-javaagent:/Users/aniu/.m2/repository/net/bytebuddy/byte-buddy-agent/1.14.13/byte-buddy-agent-1.14.13.jar' \
  -Dtest='com.rtdwh.service.*Test,com.rtdwh.controller.*Test' test
```

- V27 最终部署后再次调用窗口规则：检测 2 行、异常 0 行、状态 passed，执行证据落库成功。验收临时表 / View / 任务 / 规则 / 用户 / 角色均已精确清理（含浏览器创建规则）。后端健康 UP，部署 JAR 与测试产物 SHA-256 相同：`bbae5dcb07e3ffc5a5ba1c2950fcc4bdac2090e5907c35477bce33f8c5a86cfd`。

## 后续顺序与未完成边界

1. **V2-01 本地验收已补齐**：后续多副本协调与完整权限矩阵扩展纳入 V2-06／11；超时验证采用时间注入，未冒充 30 分钟实耗压力测试。
2. **V2-02 收尾**：持续任务真实保存点恢复已通过；异常部署提交的人工接管、环境能力与镜像认证并入 V2-06／11。参数契约、调度修订、运行配置指纹／凭证引用和执行权限检查审计已落地。旧版本仍保留兼容行为，不能视为可信历史交付。
3. **V2-03 核心控制面已落地**：交付分态、日窗口与 productionId 绑定、补数批次策略、执行 attempt 和并发回调幂等已验收。提交后重放当前关闭；仍需对具体写入模式实现并认证安全重放适配器，不能把失败重试按钮当作幂等写入证明。任意时段窗口、物理快照固定及专用人工接管入口尚未提供；日窗口质量检测已在本轮 V2-08 子包接入。
4. **V2-04 Paimon 资产基础及真实样例已落地**：新增接口的双角色 HTTP 回归已补齐；显式物理身份重绑定仍待扩展。V2-05 首个子包已实现，仍需补齐影响评估、输出/依赖迁移、未知 DDL 接管及失效扫描，再进入完整 GA 验收；随后 V2-06 运维客户端统一及伸缩；V2-07 生命周期；V2-08 质量窗口基础本轮已实施，后续补齐 SLA／通知处置／豁免；V2-09 API 发布隔离；V2-10 交互回归；V2-11 完整交付验收。V2-12 Beta 不挤占 GA。

其他明确限制：当前维护协调只面向单控制面；跨副本互斥、环境地址变更后的协调、历史未知操作人工接管、持久化会话清理重试仍属于后续运维工作。质量规则可显式配置业务窗口，全表规则仍是整体约束，不能替代窗口完整性证明；blocked 仍是受治理交付阻断，并不撤回已写入 Paimon 的数据。API 草稿／线上版本隔离尚未实现。

# RT-DWH 项目迭代：从“能跑”走向“可运营”

> 这次升级，不只是多了几个页面、几个接口。RT-DWH 正在把实时数据同步、湖仓元数据、即席查询与任务运维串成一条完整链路，让中小团队也能用更低的门槛管理一套实时数仓。

RT-DWH Management Platform 是一套面向中小团队的实时数仓管理平台，核心技术栈由 Flink CDC、Apache Paimon、Spring Boot 与 Ant Design Pro 组成。

项目一直在回答一个很实际的问题：++没有专职大数据团队，能不能让一名工程师也把实时数仓管起来？++

围绕这个目标，本轮迭代重点补齐了四条能力链：++查询工作台、Paimon 元数据治理、Flink CDC 运行链路，以及任务状态自动校准++。

## 01 为什么要做这轮升级

一个实时数仓系统，真正困难的部分并不只是“把数据同步进去”。数据进入湖仓之后，还需要知道表在哪里、字段是什么、任务是否健康、查询能否自助完成，以及故障后平台状态是否可信。

过去，这些动作往往分散在 Flink Web UI、数据库客户端、脚本与人工记录里。系统能运行，但使用者仍需要在多个工具之间来回切换。

这轮升级的方向很明确：++把分散的技术动作收拢成平台能力，把依赖经验的人工判断沉淀成系统规则++。

> RT-DWH 的目标，不是再造一套复杂的大数据平台，而是把中小团队最常用、最容易出错的实时数仓操作，做成一条看得见、管得住、能追踪的闭环。

## 02 新特性一：即席查询从输入框升级为工作台

本轮迭代对即席查询模块进行了系统性重构。它不再只是一个“输入 SQL、返回结果”的页面，而是开始具备完整的++数据探索与查询资产管理能力++。

### Catalog 可视化探索

查询页面新增 Paimon Catalog 目录树，能够按数据库、数据表、字段逐层展开，并展示字段类型、主键与可空信息。选中节点即可把表名或字段名插入编辑器，减少反复查文档和手写全限定名的成本。

### 更懂业务元数据的 SQL 编辑器

编辑器升级为 Monaco Editor，并基于平台元数据提供 Catalog、数据库、表、字段以及别名字段补全。使用 ++⌘／Ctrl＋Enter++ 即可执行查询，让查询体验更接近日常开发工具。

### SQL 资产可以保存和复用

查询脚本既可以保存在当前浏览器，也可以保存到服务端。服务端保存支持名称、描述与标签，并按用户隔离，方便个人沉淀常用取数逻辑，也避免换设备后脚本丢失。

### 查询过程更可控

平台补齐了查询历史、运行状态、主动取消、超时控制、最大返回行数、结果截断提示与 CSV 导出。后端通过 Flink SQL Gateway 管理 Session 与 Operation 生命周期，并在执行前检查 Flink Slot 情况，避免查询在资源耗尽时静默等待。

同时，查询仍坚持++只读边界++：仅允许 SELECT、SHOW、DESCRIBE、EXPLAIN、WITH 等查询语句，写入与结构变更语句会被拦截。

## 03 新特性二：Paimon 元数据管理更完整

元数据是管理平台连接“数据同步”与“数据消费”的中枢。本轮升级增强了 Paimon 元数据同步与展示，兼容 Paimon 2.x 统一 JDBC Catalog，同时保留对既有元数据结构的适配。

平台可以同步数据库、表、字段、主键、分区键与表选项，并按数据库命名规则自动识别 ODS、DWD、DWS、ADS 分层。对于已经从 Paimon 删除的表，管理库中的陈旧记录也会在同步时清理，减少“平台里看得到、湖仓里找不到”的信息漂移。

表详情进一步补充了 Schema 与 Snapshot 视图。对于本地可访问的 Warehouse，平台可以读取快照文件，展示快照编号、Schema 编号、提交时间、提交类型和变更记录，让表的演进过程更加透明。

围绕表维护，平台已经形成三类常用操作：

- Compact：合并小文件，改善读取效率；
- Expire Snapshots：按保留数量清理历史快照；
- Orphan Files Cleanup：清理不再被元数据引用的孤儿文件。

这些操作既支持单表触发，也支持按数仓分层批量执行，并留下维护日志。++元数据不再只是“展示信息”，而是开始成为可操作的治理入口++。

## 04 新特性三：Flink CDC 链路更稳，状态更可信

实时同步任务的稳定性，取决于提交前、运行中与故障后三个阶段能否形成闭环。

在提交阶段，平台增强了 MySQL、PostgreSQL 表结构探测、字段类型映射、主键识别与时区处理，并完善 Paimon Catalog、临时 Source 表以及 SQL Gateway 的运行配置。用户可以在启动任务前预览生成的 CDC SQL，提前发现配置问题。

在运行阶段，任务已经支持提交、停止、暂停、从 Savepoint 恢复、手动触发 Savepoint，并持续采集 Checkpoint、延迟与吞吐等状态指标。

本轮最新补强集中在++状态自动校准++：

- Flink 返回 NOT_FOUND 时，平台会确认 Job 已不在集群中，并把任务校准为“已终止”；
- Flink 返回 SUSPENDED 时，平台会把任务校准为“已暂停”；
- Flink 集群暂时不可达时，平台保留任务原状态，不把网络抖动误判成任务终止；
- Quartz 监控任务接入 Spring 依赖注入，禁止并发重入，并支持启停与轮询间隔配置；
- 前端任务列表每 10 秒刷新一次，页面隐藏时停止轮询，详情页同步更新任务状态。

这组变化解决的是一个很关键的问题：++平台显示的状态，必须尽可能接近 Flink 集群里的真实状态++。只有状态可信，暂停、恢复、告警与人工处置才有可靠依据。

## 05 RT-DWH 现在具备哪些核心能力

经过这轮迭代，RT-DWH 已经形成六个相互衔接的能力模块。

### 1．实时数据接入

支持 MySQL、PostgreSQL 通过 Flink CDC 实时同步到 Paimon，自动生成 CDC SQL，并提供全量后增量、增量启动、Checkpoint 与并行度等运行配置。

### 2．任务全生命周期管理

覆盖创建、提交、运行、暂停、恢复、停止、Savepoint 与状态监控，让 Flink Job 不再只能在引擎界面里单独维护。

### 3．湖仓元数据治理

自动同步 Paimon 表与字段元数据，管理 ODS、DWD、DWS、ADS 分层，查看 Schema 与 Snapshot，并执行表维护操作。

### 4．自助查询与报表

通过 Catalog 浏览、智能补全、SQL 保存、查询历史、取消、导出等能力，为开发和分析人员提供统一的即席查询入口，并衔接报表看板。

### 5．质量检查与告警

提供空值率、唯一性、范围、行数与自定义 SQL 等质量规则，检查结果可以触发钉钉、企业微信等通知通道，形成从发现到处置的闭环。

### 6．平台化运维与安全

提供系统健康检查、配置管理、JWT 鉴权、RBAC 权限、Docker Compose 与 Helm 部署能力，让平台既能快速启动，也能逐步进入规范化运行。

## 06 这次升级带来的真正变化

如果把这轮迭代压缩成一句话，就是：++RT-DWH 正从功能集合，走向一条完整的实时数仓工作流++。

数据可以通过 CDC 进入 Paimon；元数据会同步到管理平台；开发与分析人员可以在 Catalog 中找到数据并完成查询；运行任务会持续上报指标；当 Job 消失、暂停或集群暂时不可达时，平台会按照不同语义处理，而不是给出一个模糊的“异常”。

这意味着日常工作开始从“登录多个系统、人工拼接上下文”，转变为“在同一个平台里完成发现、查询、治理和运维”。对于没有专职大数据平台团队的公司，这种++降低使用门槛与认知负担++的价值，往往比堆叠更多复杂功能更重要。

## 结语

RT-DWH 仍在持续迭代，但主干能力已经越来越清晰：以 Flink CDC 负责实时接入，以 Paimon 承载湖仓数据，以管理平台连接元数据、查询、质量与运维。

下一阶段，项目将继续围绕生产可用性推进，包括完善自动化测试、强化运行指标、优化异常诊断，并让查询、质量与告警之间形成更紧密的联动。

> 真正好用的实时数仓平台，不是把复杂性展示给用户，而是把复杂性留在系统内部，把清晰、可靠和可操作交给使用者。

我是 {{作者名}}，{{简介}}。

如果你觉得今天这篇有收获，欢迎点赞、在看、转发三连，我们下篇见。

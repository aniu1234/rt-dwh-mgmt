# Reuses the Java 17 runtime already present in the local Flink image.
FROM flink:2.2.1-java17
WORKDIR /app
COPY target/rtdwh-management-*.jar app.jar
USER flink
EXPOSE 8080
ENTRYPOINT ["java", "-jar", "/app/app.jar"]
